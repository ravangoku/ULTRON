# Tools

Tools self-describe via `ToolDefinition`: name, description, risk, confirmation policy, and handler. Phase 1 registers only a constrained arithmetic calculator and limited system status. Unknown tools are rejected. Future high-impact adapters must default to preview + confirmation and cannot bypass the emergency stop.
